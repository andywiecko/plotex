terminalSettings:
  terminal: cairolatex
  terminalOptions:
    - standalone
    - transparent
  header:
    \\usepackage[T1]{fontenc}
plotSettings:
  - set colorsequence classic
  - set key noautotitle
